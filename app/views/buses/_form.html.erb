<%= form_for(bus) do |f| %>
  <% if bus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bus.errors.count, "error") %> prohibited this bus from being saved:</h2>

      <ul>
      <% bus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :busID %>
    <%= f.number_field :busID %>
  </div>

  <div class="field">
    <%= f.label :phoneNum %>
    <%= f.number_field :phoneNum %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
